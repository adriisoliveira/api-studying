name: ActionCallAPI

on:
  push:
    branches: 
      - master
    
  workflow_dispatch:

jobs:
  build:
    runs-on: dotnet-latest
    steps:
    - name: Run API
      run: |
        cd api-studying/SupermercadoAPI/src/Supermercado.API/
        dotnet run
      
