name: ActionCallAPI

on:
  push:
    branches: 
      - master
    
  workflow_dispatch:

jobs:
  build:
    runs-on: dotnet-version[3.1]
    steps:
    - name: Run API
      run: |
        dotnet run
      
